-- Add columns for forgot password feature
ALTER TABLE users
ADD COLUMN IF NOT EXISTS reset_code VARCHAR(10) DEFAULT NULL,
ADD COLUMN IF NOT EXISTS reset_code_expiry TIMESTAMP NULL DEFAULT NULL;

-- Add index for faster lookups
CREATE INDEX IF NOT EXISTS idx_reset_code ON users(reset_code);-- Add columns for forgot password feature
ALTER TABLE users
ADD COLUMN IF NOT EXISTS reset_code VARCHAR(10) DEFAULT NULL,
ADD COLUMN IF NOT EXISTS reset_code_expiry TIMESTAMP NULL DEFAULT NULL;

-- Add index for faster lookups
CREATE INDEX IF NOT EXISTS idx_reset_code ON users(reset_code);-- Add columns for forgot password feature
ALTER TABLE users
ADD COLUMN IF NOT EXISTS reset_code VARCHAR(10) DEFAULT NULL,
ADD COLUMN IF NOT EXISTS reset_code_expiry TIMESTAMP NULL DEFAULT NULL;

-- Add index for faster lookups
CREATE INDEX IF NOT EXISTS idx_reset_code ON users(reset_code);-- Add columns for forgot password feature
ALTER TABLE users
ADD COLUMN IF NOT EXISTS reset_code VARCHAR(10) DEFAULT NULL,
ADD COLUMN IF NOT EXISTS reset_code_expiry TIMESTAMP NULL DEFAULT NULL;

-- Add index for faster lookups
CREATE INDEX IF NOT EXISTS idx_reset_code ON users(reset_code);-- Add columns for forgot password feature
ALTER TABLE users
ADD COLUMN IF NOT EXISTS reset_code VARCHAR(10) DEFAULT NULL,
ADD COLUMN IF NOT EXISTS reset_code_expiry TIMESTAMP NULL DEFAULT NULL;

-- Add index for faster lookups
CREATE INDEX IF NOT EXISTS idx_reset_code ON users(reset_code);-- Add columns for forgot password feature
ALTER TABLE users
ADD COLUMN IF NOT EXISTS reset_code VARCHAR(10) DEFAULT NULL,
ADD COLUMN IF NOT EXISTS reset_code_expiry TIMESTAMP NULL DEFAULT NULL;

-- Add index for faster lookups
CREATE INDEX IF NOT EXISTS idx_reset_code ON users(reset_code);-- Add columns for forgot password feature
ALTER TABLE users
ADD COLUMN IF NOT EXISTS reset_code VARCHAR(10) DEFAULT NULL,
ADD COLUMN IF NOT EXISTS reset_code_expiry TIMESTAMP NULL DEFAULT NULL;

-- Add index for faster lookups
CREATE INDEX IF NOT EXISTS idx_reset_code ON users(reset_code);-- Add columns for forgot password feature
ALTER TABLE users
ADD COLUMN IF NOT EXISTS reset_code VARCHAR(10) DEFAULT NULL,
ADD COLUMN IF NOT EXISTS reset_code_expiry TIMESTAMP NULL DEFAULT NULL;

-- Add index for faster lookups
CREATE INDEX IF NOT EXISTS idx_reset_code ON users(reset_code);-- Add columns for forgot password feature
ALTER TABLE users
ADD COLUMN IF NOT EXISTS reset_code VARCHAR(10) DEFAULT NULL,
ADD COLUMN IF NOT EXISTS reset_code_expiry TIMESTAMP NULL DEFAULT NULL;

-- Add index for faster lookups
CREATE INDEX IF NOT EXISTS idx_reset_code ON users(reset_code);-- Add columns for forgot password feature
ALTER TABLE users
ADD COLUMN IF NOT EXISTS reset_code VARCHAR(10) DEFAULT NULL,
ADD COLUMN IF NOT EXISTS reset_code_expiry TIMESTAMP NULL DEFAULT NULL;

-- Add index for faster lookups
CREATE INDEX IF NOT EXISTS idx_reset_code ON users(reset_code);-- Add columns for forgot password feature
ALTER TABLE users
ADD COLUMN IF NOT EXISTS reset_code VARCHAR(10) DEFAULT NULL,
ADD COLUMN IF NOT EXISTS reset_code_expiry TIMESTAMP NULL DEFAULT NULL;

-- Add index for faster lookups
CREATE INDEX IF NOT EXISTS idx_reset_code ON users(reset_code);-- Add columns for forgot password feature
ALTER TABLE users
ADD COLUMN IF NOT EXISTS reset_code VARCHAR(10) DEFAULT NULL,
ADD COLUMN IF NOT EXISTS reset_code_expiry TIMESTAMP NULL DEFAULT NULL;

-- Add index for faster lookups
CREATE INDEX IF NOT EXISTS idx_reset_code ON users(reset_code);-- Add columns for forgot password feature
ALTER TABLE users
ADD COLUMN IF NOT EXISTS reset_code VARCHAR(10) DEFAULT NULL,
ADD COLUMN IF NOT EXISTS reset_code_expiry TIMESTAMP NULL DEFAULT NULL;

-- Add index for faster lookups
CREATE INDEX IF NOT EXISTS idx_reset_code ON users(reset_code);-- Add columns for forgot password feature
ALTER TABLE users
ADD COLUMN IF NOT EXISTS reset_code VARCHAR(10) DEFAULT NULL,
ADD COLUMN IF NOT EXISTS reset_code_expiry TIMESTAMP NULL DEFAULT NULL;

-- Add index for faster lookups
CREATE INDEX IF NOT EXISTS idx_reset_code ON users(reset_code);-- Add columns for forgot password feature
ALTER TABLE users
ADD COLUMN IF NOT EXISTS reset_code VARCHAR(10) DEFAULT NULL,
ADD COLUMN IF NOT EXISTS reset_code_expiry TIMESTAMP NULL DEFAULT NULL;

-- Add index for faster lookups
CREATE INDEX IF NOT EXISTS idx_reset_code ON users(reset_code);-- Add columns for forgot password feature
ALTER TABLE users
ADD COLUMN IF NOT EXISTS reset_code VARCHAR(10) DEFAULT NULL,
ADD COLUMN IF NOT EXISTS reset_code_expiry TIMESTAMP NULL DEFAULT NULL;

-- Add index for faster lookups
CREATE INDEX IF NOT EXISTS idx_reset_code ON users(reset_code);-- Add columns for forgot password feature
ALTER TABLE users
ADD COLUMN IF NOT EXISTS reset_code VARCHAR(10) DEFAULT NULL,
ADD COLUMN IF NOT EXISTS reset_code_expiry TIMESTAMP NULL DEFAULT NULL;

-- Add index for faster lookups
CREATE INDEX IF NOT EXISTS idx_reset_code ON users(reset_code);-- Add columns for forgot password feature
ALTER TABLE users
ADD COLUMN IF NOT EXISTS reset_code VARCHAR(10) DEFAULT NULL,
ADD COLUMN IF NOT EXISTS reset_code_expiry TIMESTAMP NULL DEFAULT NULL;

-- Add index for faster lookups
CREATE INDEX IF NOT EXISTS idx_reset_code ON users(reset_code);-- Add columns for forgot password feature
ALTER TABLE users
ADD COLUMN IF NOT EXISTS reset_code VARCHAR(10) DEFAULT NULL,
ADD COLUMN IF NOT EXISTS reset_code_expiry TIMESTAMP NULL DEFAULT NULL;

-- Add index for faster lookups
CREATE INDEX IF NOT EXISTS idx_reset_code ON users(reset_code);-- Add columns for forgot password feature
ALTER TABLE users
ADD COLUMN IF NOT EXISTS reset_code VARCHAR(10) DEFAULT NULL,
ADD COLUMN IF NOT EXISTS reset_code_expiry TIMESTAMP NULL DEFAULT NULL;

-- Add index for faster lookups
CREATE INDEX IF NOT EXISTS idx_reset_code ON users(reset_code);-- Add columns for forgot password feature
ALTER TABLE users
ADD COLUMN IF NOT EXISTS reset_code VARCHAR(10) DEFAULT NULL,
ADD COLUMN IF NOT EXISTS reset_code_expiry TIMESTAMP NULL DEFAULT NULL;

-- Add index for faster lookups
CREATE INDEX IF NOT EXISTS idx_reset_code ON users(reset_code);-- Add columns for forgot password feature
ALTER TABLE users
ADD COLUMN IF NOT EXISTS reset_code VARCHAR(10) DEFAULT NULL,
ADD COLUMN IF NOT EXISTS reset_code_expiry TIMESTAMP NULL DEFAULT NULL;

-- Add index for faster lookups
CREATE INDEX IF NOT EXISTS idx_reset_code ON users(reset_code);-- Add columns for forgot password feature
ALTER TABLE users
ADD COLUMN IF NOT EXISTS reset_code VARCHAR(10) DEFAULT NULL,
ADD COLUMN IF NOT EXISTS reset_code_expiry TIMESTAMP NULL DEFAULT NULL;

-- Add index for faster lookups
CREATE INDEX IF NOT EXISTS idx_reset_code ON users(reset_code);-- Add columns for forgot password feature
ALTER TABLE users
ADD COLUMN IF NOT EXISTS reset_code VARCHAR(10) DEFAULT NULL,
ADD COLUMN IF NOT EXISTS reset_code_expiry TIMESTAMP NULL DEFAULT NULL;

-- Add index for faster lookups
CREATE INDEX IF NOT EXISTS idx_reset_code ON users(reset_code);-- Add columns for forgot password feature
ALTER TABLE users
ADD COLUMN IF NOT EXISTS reset_code VARCHAR(10) DEFAULT NULL,
ADD COLUMN IF NOT EXISTS reset_code_expiry TIMESTAMP NULL DEFAULT NULL;

-- Add index for faster lookups
CREATE INDEX IF NOT EXISTS idx_reset_code ON users(reset_code);-- Add columns for forgot password feature
ALTER TABLE users
ADD COLUMN IF NOT EXISTS reset_code VARCHAR(10) DEFAULT NULL,
ADD COLUMN IF NOT EXISTS reset_code_expiry TIMESTAMP NULL DEFAULT NULL;

-- Add index for faster lookups
CREATE INDEX IF NOT EXISTS idx_reset_code ON users(reset_code);-- Add columns for forgot password feature
ALTER TABLE users
ADD COLUMN IF NOT EXISTS reset_code VARCHAR(10) DEFAULT NULL,
ADD COLUMN IF NOT EXISTS reset_code_expiry TIMESTAMP NULL DEFAULT NULL;

-- Add index for faster lookups
CREATE INDEX IF NOT EXISTS idx_reset_code ON users(reset_code);-- Add columns for forgot password feature
ALTER TABLE users
ADD COLUMN IF NOT EXISTS reset_code VARCHAR(10) DEFAULT NULL,
ADD COLUMN IF NOT EXISTS reset_code_expiry TIMESTAMP NULL DEFAULT NULL;

-- Add index for faster lookups
CREATE INDEX IF NOT EXISTS idx_reset_code ON users(reset_code);